import { UIContent } from '@/components/content';
import { UIRoot } from '@/components/root';
import { UIWrapper } from '@/components/wrapper';

export const UIFormLogin = {
  Root: UIRoot,
  Wrapper: UIWrapper,
  Content: UIContent,
};
